# 远程WVP运行目录完整检查报告

## 检查时间
2025-12-18 10:20

## 一、宿主机目录结构 (`/home/wvp/`)

### 1.1 主目录内容

```
/home/wvp/
├── build/               # 构建目录（14个子目录）
├── config/              # 配置文件目录（空）
├── docker/              # Docker相关文件（6个子目录）
│   ├── logs/           # 日志目录
│   ├── media/          # 媒体服务器配置
│   ├── volumes/        # 数据卷目录
│   └── wvp/            # WVP配置
│       ├── config/     # 配置文件目录
│       └── wvp/        # Spring Boot配置文件
│           ├── application.yml
│           ├── application-base.yml
│           └── application-docker.yml
├── logs/                # 日志目录（空）
├── static/              # 前端静态文件目录 ✅
│   ├── index.html       # 前端入口文件 (7.3K)
│   ├── favicon.ico      # 网站图标 (1.2K)
│   ├── libDecoder.wasm  # WebAssembly文件 (2.3M)
│   └── static/          # 静态资源目录
│       ├── css/        # CSS文件
│       └── js/         # JavaScript文件
└── wvp.jar              # 后端JAR文件 (126M) ✅
```

### 1.2 前端文件统计

- **文件总数**: 237个文件（宿主机）/ 238个文件（容器内）
- **主要文件**:
  - `index.html` - 7.3K
  - `favicon.ico` - 1.2K
  - `libDecoder.wasm` - 2.3M
  - `static/` 目录 - 包含CSS和JS文件

### 1.3 目录大小

- `wvp.jar`: 126M
- `static/`: 约2.4M
- `build/`: 约4.0K（目录）
- `docker/`: 约235（目录）

## 二、容器内目录结构 (`/opt/wvp/`)

### 2.1 主目录内容

```
/opt/wvp/
├── build/               # 构建目录（从宿主机挂载）
├── config/              # 配置文件目录（从宿主机挂载）
├── docker/              # Docker相关文件（从宿主机挂载）
├── logs/                # 日志目录（从宿主机挂载）
├── static/              # 前端静态文件目录（从宿主机挂载）✅
│   ├── index.html       # 前端入口文件
│   ├── favicon.ico      # 网站图标
│   ├── libDecoder.wasm  # WebAssembly文件
│   └── static/          # 静态资源目录
└── wvp.jar              # 后端JAR文件（从宿主机挂载）✅
```

### 2.2 工作目录

- **容器工作目录**: `/opt/ylcx`
- **配置文件路径**: `/opt/ylcx/wvp/application-docker.yml`

## 三、挂载点配置

### 3.1 完整挂载列表

| 序号 | 宿主机路径 | 容器内路径 | 类型 | 说明 |
|-----|-----------|-----------|------|------|
| 1 | `/home/wvp` | `/opt/wvp` | bind | 主目录挂载（包含static、wvp.jar、build等） |
| 2 | `/home/wvp/docker/wvp/config` | `/opt/wvp/config` | bind | 配置文件目录 |
| 3 | `/home/wvp/docker/wvp/wvp/` | `/opt/ylcx/wvp/` | bind | Spring Boot配置文件目录 |
| 4 | `/home/wvp/docker/logs/wvp` | `/opt/wvp/logs` | bind | 日志目录 |

### 3.2 挂载说明

- **主目录挂载** (`/home/wvp:/opt/wvp`): 包含所有主要文件
  - 前端文件: `/home/wvp/static` = `/opt/wvp/static`
  - JAR文件: `/home/wvp/wvp.jar` = `/opt/wvp/wvp.jar`
  - 构建目录: `/home/wvp/build` = `/opt/wvp/build`

- **子目录挂载**: 覆盖主目录挂载中的对应子目录
  - `config/`: 独立挂载，覆盖主目录中的config
  - `logs/`: 独立挂载，覆盖主目录中的logs

## 四、配置文件位置

### 4.1 Spring Boot配置文件

**宿主机**: `/home/wvp/docker/wvp/wvp/`
**容器内**: `/opt/ylcx/wvp/`

**配置文件**:
- `application.yml` - 主配置文件（67字节）
- `application-base.yml` - 基础配置（3.0K）
- `application-docker.yml` - Docker环境配置（6.7K）✅

### 4.2 配置文件内容

`application-docker.yml` 包含：
- Redis配置
- MySQL数据库配置（通过环境变量）
- 静态资源路径配置：`web.static.path: /opt/wvp/static`
- JT1078配置
- SIP配置
- 媒体服务器配置

## 五、环境变量配置

### 5.1 数据库环境变量

- `DATABASE_HOST=polaris-mysql`
- `DATABASE_PORT=3306`
- `DATABASE_USER=wvp_user`
- `DATABASE_PASSWORD=wvp_password`

### 5.2 Redis环境变量

- `REDIS_HOST=polaris-redis`
- `REDIS_PORT=6379`
- `SPRING_DATA_REDIS_HOST=polaris-redis`
- `SPRING_DATA_REDIS_PORT=6379`
- `SPRING_DATA_REDIS_DATABASE=1`

### 5.3 静态资源环境变量

- `WEB_STATIC_PATH`: 未设置（使用默认值 `/opt/wvp/static`）

## 六、服务运行状态

### 6.1 容器状态

- **容器名称**: `polaris-wvp`
- **状态**: Running
- **工作目录**: `/opt/ylcx`
- **启动命令**: `java -Xms512m -Xmx1024m ... -jar /opt/wvp/wvp.jar --spring.config.location=file:/opt/ylcx/wvp/`

### 6.2 服务验证

- ✅ MySQL数据库连接成功
- ✅ Redis连接成功
- ✅ 前端文件可访问（HTTP 200）
- ✅ API接口正常
- ✅ 静态资源目录配置正确：`使用外部静态资源目录: /opt/wvp/static/`

## 七、关键路径总结

### 7.1 前端文件路径

- **宿主机**: `/home/wvp/static/`
- **容器内**: `/opt/wvp/static/`
- **配置路径**: `/opt/wvp/static`（在application-docker.yml中）

### 7.2 后端文件路径

- **JAR文件（宿主机）**: `/home/wvp/wvp.jar`
- **JAR文件（容器内）**: `/opt/wvp/wvp.jar`
- **启动命令**: `java -jar /opt/wvp/wvp.jar`

### 7.3 配置文件路径

- **宿主机**: `/home/wvp/docker/wvp/wvp/application-docker.yml`
- **容器内**: `/opt/ylcx/wvp/application-docker.yml`
- **启动参数**: `--spring.config.location=file:/opt/ylcx/wvp/`

### 7.4 日志文件路径

- **宿主机**: `/home/wvp/docker/logs/wvp/`
- **容器内**: `/opt/wvp/logs/`

## 八、目录权限

### 8.1 宿主机权限

- `/home/wvp/`: `drwxrwxrwx` (777)
- `/home/wvp/static/`: `drwxr-xr-x` (755)
- `/home/wvp/wvp.jar`: `-rwxr--r--` (744)

### 8.2 容器内权限

- `/opt/wvp/`: `drwxrwxrwx` (777)
- `/opt/wvp/static/`: `drwxr-xr-x` (755)
- `/opt/wvp/wvp.jar`: `-rwxr--r--` (744)

## 九、注意事项

1. **主目录挂载**: `/home/wvp:/opt/wvp` 包含了所有子目录
2. **子目录挂载**: `config` 和 `logs` 子目录有独立挂载，会覆盖主目录挂载
3. **前端文件**: 位于 `/home/wvp/static/`，通过主目录挂载访问
4. **配置文件**: 位于 `/opt/ylcx/wvp/`，通过独立挂载访问
5. **工作目录**: 容器工作目录是 `/opt/ylcx`，不是 `/opt/wvp`

## 十、建议

1. ✅ 当前配置正确，前后端分离已完成
2. ✅ 前端文件位置正确：`/opt/wvp/static`
3. ✅ 挂载配置合理，主目录挂载包含所有必要文件
4. ⚠️ 注意：`build/` 目录也在主目录挂载中，如果不需要可以排除

