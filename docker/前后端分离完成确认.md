# WVP前后端分离完成确认

## ✅ 前后端分离已成功完成

### 完成状态

1. **前端代码已部署**
   - 位置：`/home/wvp/static`（宿主机）= `/opt/wvp/static`（容器内）
   - 文件数量：237个文件
   - 包含：index.html, favicon.ico, libDecoder.wasm, static/目录等

2. **服务配置已更新**
   - Spring Boot静态资源路径：`/opt/wvp/static`
   - WebMvcConfig已正确配置
   - application-docker.yml已更新

3. **挂载配置已优化**
   - 主目录挂载：`/home/wvp:/opt/wvp`
   - 移除了冲突的子目录挂载
   - 前端文件可通过主目录挂载访问

4. **服务运行正常**
   - ✅ 服务启动成功
   - ✅ 前端页面可访问（HTTP 200）
   - ✅ API接口正常
   - ✅ 静态资源可访问

### 目录结构

```
宿主机: /home/wvp/
├── static/              # 前端静态文件（237个文件）
│   ├── index.html       # 前端入口文件 ✅
│   ├── favicon.ico      # 网站图标 ✅
│   ├── libDecoder.wasm  # WebAssembly文件 ✅
│   └── static/          # 静态资源目录
│       ├── css/         # CSS文件
│       ├── js/          # JavaScript文件
│       └── ...
├── wvp.jar              # 后端JAR文件 ✅
├── config/              # 配置文件目录
└── logs/                # 日志目录

容器内: /opt/wvp/
├── static/              # 前端静态文件（从宿主机挂载）✅
├── wvp.jar              # 后端JAR文件 ✅
├── config/              # 配置文件目录
└── logs/                # 日志目录
```

### 挂载配置

```bash
-v /home/wvp:/opt/wvp                    # 主目录挂载（包含static目录）
-v /home/wvp/docker/wvp/config:/opt/wvp/config
-v /home/wvp/docker/wvp/wvp/:/opt/ylcx/wvp/
-v /home/wvp/docker/logs/wvp:/opt/wvp/logs
```

**注意**：已移除 `/home/wvp/docker/volumes/wvp/static:/opt/wvp/static` 挂载，避免与主目录挂载冲突。

### 配置详情

#### 1. 前端构建配置 (`web/vue.config.js`)

```javascript
outputDir: "dist",  // 构建输出到dist目录（前后端分离）
assetsDir: "static",  // 静态资源目录
```

#### 2. Spring Boot配置 (`application-docker.yml`)

```yaml
web:
    static:
        path: ${WEB_STATIC_PATH:/opt/wvp/static}
```

#### 3. WebMvcConfig配置

```java
// 默认路径：/opt/wvp/static（容器内路径，对应宿主机：/home/wvp/static）
String defaultPath = "/opt/wvp/static";
```

### 访问方式

- **前端页面**: http://172.31.127.47:18978/
- **API接口**: http://172.31.127.47:18978/api/
- **静态资源**: http://172.31.127.47:18978/static/

### 前端更新流程

1. **本地构建**
   ```bash
   cd web/
   npm run build:prod
   ```

2. **打包上传**
   ```bash
   tar czf /tmp/wvp-frontend.tar.gz -C web/dist .
   scp /tmp/wvp-frontend.tar.gz root@172.31.127.47:/tmp/
   ```

3. **远程部署**
   ```bash
   ssh root@172.31.127.47
   cd /home/wvp
   rm -rf static/*
   tar xzf /tmp/wvp-frontend.tar.gz -C static
   chmod -R 755 static
   ```

4. **验证更新**
   - 前端文件更新后，Spring Boot会自动读取新文件（无需重启容器）
   - 刷新浏览器即可看到更新

### 优势

- ✅ **前后端完全分离**：前端代码独立部署，不依赖JAR包
- ✅ **热更新支持**：更新前端文件无需重启容器
- ✅ **独立开发**：前后端可以独立开发和维护
- ✅ **便于CI/CD**：前端和后端可以分别进行CI/CD
- ✅ **灵活部署**：前端可以部署到CDN或Nginx

### 验证结果

- ✅ 前端文件已部署：237个文件
- ✅ 文件位置正确：`/home/wvp/static` = `/opt/wvp/static`
- ✅ 服务启动成功：`Started VManageBootstrap`
- ✅ 前端页面可访问：HTTP 200
- ✅ 静态资源可访问：HTTP 200
- ✅ 挂载配置正确：宿主机和容器内文件同步

### 总结

前后端分离配置已完成，前端运行代码已成功部署到 `/opt/wvp/static` 目录（容器内）= `/home/wvp/static` 目录（宿主机）。服务正常运行，前端页面和API接口均可正常访问。

