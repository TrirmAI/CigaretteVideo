# 远程WVP运行目录检查报告

## 检查时间
2025-12-18

## 目录结构概览

### 宿主机目录结构 (`/home/wvp/`)

```
/home/wvp/
├── static/              # 前端静态文件目录
├── wvp.jar              # 后端JAR文件
├── config/              # 配置文件目录（挂载）
├── logs/                # 日志目录（挂载）
├── build/               # 构建目录
└── docker/              # Docker相关文件
    ├── wvp/
    │   ├── config/      # 配置文件目录
    │   └── wvp/         # Spring Boot配置文件
    │       ├── application.yml
    │       └── application-docker.yml
    └── logs/
        └── wvp/         # 日志文件目录
```

### 容器内目录结构 (`/opt/wvp/`)

```
/opt/wvp/
├── static/              # 前端静态文件（从宿主机挂载）
├── wvp.jar              # 后端JAR文件（从宿主机挂载）
├── config/              # 配置文件目录（从宿主机挂载）
├── logs/                # 日志目录（从宿主机挂载）
└── ...
```

## 详细检查结果

### 1. 前端文件目录

**宿主机**: `/home/wvp/static/`
**容器内**: `/opt/wvp/static/`

**文件数量**: 237个文件

**主要文件**:
- `index.html` - 前端入口文件
- `favicon.ico` - 网站图标
- `libDecoder.wasm` - WebAssembly文件
- `static/` - 静态资源目录
  - `css/` - CSS文件
  - `js/` - JavaScript文件

### 2. 后端JAR文件

**宿主机**: `/home/wvp/wvp.jar`
**容器内**: `/opt/wvp/wvp.jar`

**文件大小**: 约126MB

### 3. 配置文件

**宿主机**: `/home/wvp/docker/wvp/wvp/`
**容器内**: `/opt/ylcx/wvp/`

**配置文件**:
- `application.yml` - 主配置文件
- `application-docker.yml` - Docker环境配置

### 4. 日志目录

**宿主机**: `/home/wvp/docker/logs/wvp/`
**容器内**: `/opt/wvp/logs/`

### 5. 挂载点配置

| 宿主机路径 | 容器内路径 | 说明 |
|-----------|-----------|------|
| `/home/wvp` | `/opt/wvp` | 主目录挂载（包含static、wvp.jar） |
| `/home/wvp/docker/wvp/config` | `/opt/wvp/config` | 配置文件目录 |
| `/home/wvp/docker/wvp/wvp/` | `/opt/ylcx/wvp/` | Spring Boot配置文件 |
| `/home/wvp/docker/logs/wvp` | `/opt/wvp/logs` | 日志目录 |

### 6. 环境变量

- `WEB_STATIC_PATH`: `/opt/wvp/static`（默认）
- `DATABASE_HOST`: `polaris-mysql`
- `DATABASE_PORT`: `3306`
- `DATABASE_USER`: `wvp_user`
- `DATABASE_PASSWORD`: `wvp_password`
- `REDIS_HOST`: `polaris-redis`
- `REDIS_PORT`: `6379`

## 运行状态

- ✅ 容器运行正常
- ✅ 前端文件已部署
- ✅ 后端JAR文件存在
- ✅ 配置文件已加载
- ✅ 日志目录可访问
- ✅ 挂载配置正确

## 注意事项

1. **前端文件位置**: `/home/wvp/static/` = `/opt/wvp/static/`
2. **配置文件位置**: `/home/wvp/docker/wvp/wvp/` = `/opt/ylcx/wvp/`
3. **日志文件位置**: `/home/wvp/docker/logs/wvp/` = `/opt/wvp/logs/`
4. **主目录挂载**: `/home/wvp` 挂载到 `/opt/wvp`，包含所有子目录

